# Model and capability panel

The panel above the conversation controls the active model and capability selection for the work at hand. Use it to make the tool surface visible before you send a request.

Choose the model and only the capabilities required for the next task. Changing these controls does not alter existing files; it affects how the next Agent turn can reason and which tools it may use.
