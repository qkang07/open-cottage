# Models, presets, and API keys

Models are configured in the browser through providers, endpoints, keys, and model names. A provider may offer a discoverable model list, but manual model names remain valid when discovery is unavailable.

API keys are not saved to the workspace. Presets and provider settings follow the browser origin, while the active workspace selection and task context follow the folder. This keeps credentials separate from files that may be backed up or shared.
