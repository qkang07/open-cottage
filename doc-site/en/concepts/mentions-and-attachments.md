# @ references and attachments

Type `@` in the composer to choose a file from the current workspace. This makes a specific source file part of the immediate task context without moving it elsewhere.

Attachments add supporting material. State how the Agent should use each attachment and which workspace path should receive the final result. References and attachments do not expand the Agent's folder permission beyond the workspace you authorized.
