# Workspaces

A workspace is the local folder you authorize in the browser. It is the Agent's file boundary and the home for source material, results, workspace-specific conversations, attachments, settings, and optional external packs under `.cottage/`.

Choose a separate folder when a project, client delivery, or material collection needs separate context. Changing folders does not delete the old workspace; it changes which folder and workspace state the current session uses.
