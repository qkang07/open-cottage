# Configuration reference

Configuration is divided between browser-origin settings and workspace settings. Provider credentials remain browser-local; workspace state belongs to `.cottage/` in the authorized folder.

Use the Settings interface for normal configuration. Preserve a saved value when a provider, service, or model-list lookup fails so that the user can correct it deliberately.
