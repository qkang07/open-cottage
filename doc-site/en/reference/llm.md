# Model-provider reference

Model providers are configured with an endpoint, credentials where required, and a model name. The provider's current API and the model configuration interface are authoritative for supported fields.

Keys are stored in the browser rather than written into a workspace. See [Model providers and API keys](/en/guide/models-and-keys) for the user workflow.
