# Platform vision

Open Cottage aims to make Agent work trustworthy over time: natural-language intent should become a result that can be inspected, approved, and verified in the user's own environment.

The product centers the local workspace rather than a remote project migration. It combines explicit directory authorization, visible tool activity, previews, staged review, sensitive-action approval, and user-owned files so that convenient automation remains accountable.
