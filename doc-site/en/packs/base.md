# Core file capabilities

Core file capabilities are available without enabling a specialized pack. They let the Agent work with material inside the folder you authorize, subject to the task boundaries and review policy you set.

Use `@` references, previews, and staged diffs to keep this basic workflow specific and inspectable.
