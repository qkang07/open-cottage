# Image generation

Enable **Image generation** when a task needs a new raster image or an edit to an existing workspace image. Describe the intended subject, style, dimensions, placement, and output path.

Inspect the generated file in the workspace before using it. For product icons, logos, or visuals better expressed as SVG or UI code, prefer the existing design system instead of generating a bitmap asset.
