# Code changes

The **Code changes** pack is for repository and source-file work. Give it an explicit objective, permitted files, constraints, and output or validation expectations. Keep staged review on so the resulting diffs are examined before application.

Use plan mode for changes that cross modules, public APIs, routes, or several files. The pack helps the Agent perform the work; it does not replace project-specific review, tests, or deployment checks.
