# Toolkit feature

现在流行的 agent 架构是，有限的原生工具，然后通过 skills 来扩展。
因为这些有限的原生工具里，包含了 cli 和代码执行啊，这其实是无限放大了工具类目。
所以很多想和 AI 集成的软件，开始搞 cli，把自己的 api 注册到 shell 环境中，这样 AI 加载一个 skill 就能使用。
部分原因是，shell 命令已经被 AI 训练到记忆里去了，调用起来更‘原生’。

我不知道这样好不好。