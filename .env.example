# Open Cottage — 环境变量示例（复制为 .env.local 后按需修改）

# --- 公开 API LLM（默认路径）---
# 模型配置写入工作空间 .cottage/config.json
# API Key 在应用内「设置」填写，仅存浏览器 IndexedDB

# LLM 代理（二选一）
#
# 方式 B：Cottage Service HTTP 代理
# 启动 cottage-service-go 后，前端「设置 → Cottage Service」连接，
# 再在「模型设置与 API」中启用 API 代理。API Key 仍由浏览器持有并转发。
