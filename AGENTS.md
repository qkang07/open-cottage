# Agent 指南

## 参考代码（Reference-only）

仓库根目录下的 `opencode/`（以及 `opencode-*/`、`reference/` 等）是**第三方项目源码**，
仅作为**参考资料**下载到本地，用于查阅设计思路、对照实现、启发灵感。

对参考代码的硬性约束：

- **只读参考**：不得修改、不得复制其代码进本项目目录、不得作为本项目的一部分构建或运行。
- **不提交**：这些目录已加入 `.gitignore`，禁止 `git add`、禁止提交、禁止打包进任何产物。
- **不引用为依赖**：不得在 `package.json` / `tsconfig` paths / import 路径中将其接入本项目。
- **借鉴需合规**：若从中借鉴实现思路，需在本项目代码中以自己的方式重写，并遵守其原始 License；若 License 不允许借鉴，则完全不参考。
- **回答用户问题**：当用户问到 opencode 相关内容，可读取该目录作答；但任何改动只作用于本项目代码，绝不修改参考目录。

## 隐藏功能（Hidden Features）

部分能力已在代码中实现但**暂时对用户隐藏/未接入 UI**（如 Python 数据分析、计划闸门、**任务模式 / TaskPanel**、子任务派生、**编排模式 / Orchestration** 等）。版本历史已重新实现为公开 Beta，但默认按工作区关闭。
完整清单见 [`docs/hidden-features.md`](docs/hidden-features.md)。

工作时的约束：

- **不受干扰**：分析现状、回答问题或规划新功能时，不要把这些隐藏/未接线的能力当作“当前生效的用户功能”描述；需明确区分“代码存在”与“产品可用”。
- **改动前先查阅**：若改动可能触及上述模块，先对照该文档确认其隐藏状态与恢复方式，避免误将隐藏能力“自作主张”地重新接出或删除。
- **保持同步**：若新增或改变了某项隐藏能力的隐藏/恢复方式，应同步更新 `docs/hidden-features.md`。

## 其它约定

- **先全貌、后改动**：任何代码、配置、交互或数据改动前，必须先检查对应功能的完整影响范围，而不能仅根据当前报错或界面现象做局部修补。至少应梳理相关的数据与状态流、所有调用/配置入口、相邻交互、失败与回退路径，以及旧数据兼容和迁移影响。
- **跨入口一致性**：同一能力存在多个入口、管理页或运行时路径时，改动必须确认它们的行为保持一致；若有意不同，须明确说明原因和边界。
- **避免静默替代**：涉及用户已有配置、选择或数据时，不得因加载失败、连接失效或列表变化而自动替换为默认值；应保留原值并让用户明确修正，除非产品需求已明确授权自动迁移。
- 任务完成时不要自动执行 `tsc` / `lint` / `build` / `test`，用户会自行执行。
- 默认使用简体中文回复。
